syntax = "proto2";

package common_msgs.sensor_msgs;


import "std_msgs/Header.proto";

message TimeReference {
  optional Header header = 1;
  optional time time_ref = 2;
  optional string source = 3;
}
