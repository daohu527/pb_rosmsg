syntax = "proto2";

package std_msgs;

message JoyFeedback {
  enum {
    TYPE_LED    = 0;
    TYPE_RUMBLE = 1;
    TYPE_BUZZER = 2;
  };

  optional uint32 type = 1;
  optional uint32 id = 2;
  optional float intensity = 3;
}
