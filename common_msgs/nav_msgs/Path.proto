syntax = "proto2";

package common_msgs.nav_msgs;

import "std_msgs/Header.proto";
import "common_msgs/geometry_msgs/PoseStamped.proto";

message Path {
  optional std_msgs.Header header = 1;
  repeated common_msgs.geometry_msgs.PoseStamped poses = 2;
}
