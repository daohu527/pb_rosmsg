syntax = "proto2";

package common_msgs.geometry_msgs;

import "common_msgs/geometry_msgs/Vector3.proto";

message Accel {
  optional common_msgs.geometry_msgs.Vector3 linear = 1;
  optional common_msgs.geometry_msgs.Vector3 angular = 2;
}
